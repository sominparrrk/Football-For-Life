import React from "react";
import styled from "styled-components";

const Container = styled.div`
  width: 100vw;
  height: 100vh;
  background-color: ${(props) => props.theme.mainLight};
`;

const Home = () => {
  return <Container>sdfsd</Container>;
};

export default Home;
